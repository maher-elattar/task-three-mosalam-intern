# Production overlay for real domains.
# Adds the letsencrypt certResolver to Docker-discovered HTTPS routers.

services:
  front:
    labels:
      - traefik.http.routers.frontend.tls.certresolver=letsencrypt
  api:
    labels:
      - traefik.http.routers.backend-api.tls.certresolver=letsencrypt
