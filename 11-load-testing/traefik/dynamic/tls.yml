# Local TLS certificate configuration.
# The certificate is generated by scripts/generate-local-certs.sh.
tls:
  certificates:
    - certFile: /certs/local.crt
      keyFile: /certs/local.key
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: false

