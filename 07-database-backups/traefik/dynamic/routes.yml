# Active HTTPS routes plus proxy security middleware.
# Traefik calls the private auth service before forwarding API traffic.

http:
  routers:
    frontend:
      rule: Host(`app.localhost`)
      entryPoints: [websecure]
      service: frontend
      tls: {}
    api:
      rule: Host(`api.localhost`)
      entryPoints: [websecure]
      service: api
      middlewares:
        - api-key-auth
        - api-rate-limit
        - api-security-headers
      tls: {}

  middlewares:
    api-key-auth:
      forwardAuth:
        address: http://auth:8000/auth
        trustForwardHeader: true
        authResponseHeaders:
          - X-Authenticated-By
    api-rate-limit:
      rateLimit:
        average: 5
        period: 1s
        burst: 10
    api-security-headers:
      headers:
        contentTypeNosniff: true
        browserXssFilter: true
        stsSeconds: 31536000

  services:
    frontend:
      loadBalancer:
        servers:
          - url: http://front:80
    api:
      loadBalancer:
        servers:
          - url: http://task03_stage07_backups-api-1:8000
          - url: http://task03_stage07_backups-api-2:8000

